component pss_top {

	action root_a {
		string instn;
		rand int lmul;

		exec pre_solve {
			instn = "vsub_vv";
		}

		constraint {
			if (instn in ["vadd_vv", "vsub_vv"]) lmul == 4;
			else lmul == 8;
		}
		
        exec body ASM =
		"""instn={{instn}}, lmul={{lmul}}""";

	}
}

